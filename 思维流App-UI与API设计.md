# 思维流 App —— UI 线框图 + API 设计

说明:全部功能均为**用户手动操作**触发(手动分支、手动打标签、手动关联思维线),
不包含任何 NLP / AI 自动识别、自动分类、自动关联功能。

核心只保留三种关系,不做更多:
- `parent_id`:这个念头从哪里分叉出来(结构)
- `thread_id`:它属于哪条跨时间的思维线(时间/主题)
- `tag_id`:事后手动打的标记(性质,可选)

不设 `stage`(起念/期待/发生/评价这类阶段分类已删除)——很多长期主题
(学日语、做量化、要不要辞职)根本无法干净地套进这几个阶段,强行分类
既不准确,又增加记录负担,与"记录时零摩擦"的原则冲突。

---

## 一、记录页(App 默认打开页)

```
┌─────────────────────────────────┐
│  今天                8月26日 · 7条 │
├─────────────────────────────────┤
│                                  │
│ 09:12  先随便记记,今天感觉一般 ⎇ ⚭│
│ ──────────────────────────────  │
│ 09:40  10月3日去看演唱会      ⎇ ⚭│
│  🔗 演唱会                       │
│  │                               │
│  ├─ 10:05  因为B更快能看到效果 ⎇ ⚭│
│  │    │                          │
│  │    └─ 10:40  那就先定第一步 ⎇ ⚭│
│  │                               │
│  └─ 10:20  不过也可能三分钟热度⎇ ⚭│
│      [情绪化]                    │
│ ──────────────────────────────  │
│ 11:15  中午又觉得A也没那么差   ⎇ ⚭│
│                                  │
├─────────────────────────────────┤
│ 最近: [学日语] [演唱会] [要不要辞职]│
│ [ 此刻在想什么...          ] (➤) │
└─────────────────────────────────┘

⎇ = 分支按钮(点击在该条下新建缩进子记录)
⚭ = 关联按钮(点击打开完整Thread选择器,见三点五节)
🔗 演唱会 = 已关联Thread时,文字下方显示可点击的胶囊(不再显示阶段),
   点击跳转到该Thread的详情页(见第三节)
[标签] = 事后手动补打,与分支/关联无关,保持可选
"最近: xxx" = 输入框上方常驻2-3个最近活跃的Thread快捷胶囊,
   点击直接把这条新记录关联上去,不需要打开选择器
```

### UI 设计提示词
```
设计一个极简的手机记事App主页,风格类似聊天时间轴:
- 纵向排列的文本条目,每条前面是灰色小号时间戳,右侧是内容
- 可以有缩进层级(类似大纲/思维导图),缩进由竖线连接父子条目
- 每条右侧有两个小图标按钮:一个"分支"(git-branch风格),一个"关联"
  (link/chain风格),两个图标并排,视觉权重相同但用不同图形区分
- 如果某条记录已经关联了一条Thread,在正文下方显示一个可点击的小胶囊,
  内容只是Thread的名字(不带阶段/分类文字),点击后跳转到该Thread的详情页
- 输入框正上方常驻一行2-3个"最近使用"的Thread快捷胶囊,点击可以让
  即将发送的这条记录直接关联上去,免去搜索和多级选择
- 底部固定单行输入框+发送按钮,没有其他导航栏或装饰元素
- 扁平设计,大量留白,无渐变无阴影,中性色为主,标签/关联胶囊用彩色点缀
```

---

## 二、回顾页

```
┌─────────────────────────────────┐
│  ←  回顾                         │
├─────────────────────────────────┤
│ 按标签                            │
│ ┌───────────────────────────┐   │
│ │ [跟风] 5条              ˅  │   │
│ │  8/19 09:40 想换方向做B     │   │
│ │  8/21 21:02 看到朋友做副业  │   │
│ │  8/24 14:15 又想换方向了    │   │
│ ├───────────────────────────┤   │
│ │ [认真规划] 3条          >  │   │
│ ├───────────────────────────┤   │
│ │ [情绪化] 4条            >  │   │
│ └───────────────────────────┘   │
│                                  │
│ 按日期                            │
│ ┌───────────────────────────┐   │
│ │ 8月26日            7条  >  │   │
│ ├───────────────────────────┤   │
│ │ 8月25日            4条  >  │   │
│ ├───────────────────────────┤   │
│ │ 8月24日            9条  >  │   │
│ └───────────────────────────┘   │
│                                  │
│ 按思维线                          │
│ ┌───────────────────────────┐   │
│ │ 演唱会              4条 >  │   │
│ ├───────────────────────────┤   │
│ │ 学日语             12条 >  │   │
│ ├───────────────────────────┤   │
│ │ 要不要辞职          6条 >  │   │
│ └───────────────────────────┘   │
└─────────────────────────────────┘
```

新增第三个分组"按思维线",和"按标签""按日期"是并列的三个独立入口,
点击进入某条Thread即为第三节的详情页。

### UI 设计提示词
```
设计一个"回顾"列表页,上中下三个分组,每组是一个圆角卡片容器:
- 第一组"按标签",列表项左侧是彩色标签胶囊+名字,右侧是数量和展开图标,
  点击原地展开成缩进的记录预览列表
- 第二组"按日期",列表项是日期+数量+右箭头,点击跳转回那天的记录页
- 第三组"按思维线",列表项是Thread名字+记录总数+右箭头,点击跳转到
  该Thread的详情时间线页
- 列表行之间用极细分隔线,不用卡片阴影,风格与主页一致的扁平极简
```

---

## 三、思维线详情页(从记录页某条点入,或从回顾页"按思维线"点入)

```
┌─────────────────────────────────┐
│  ←  演唱会                        │
│  共 4 条记录 · 跨度 39 天          │
├─────────────────────────────────┤
│                                  │
│  ●  8月26日                       │
│  │  10月3日去看演唱会。            │
│  │                                │
│  │      17天后                    │
│  │                                │
│  ●  9月12日                       │
│  │  开始期待演唱会了。              │
│  │                                │
│  │      21天后                    │
│  │                                │
│  ●  10月3日                       │
│  │  今天真的去看了。                │
│  │                                │
│  │      1天后                     │
│  │                                │
│  ●  10月4日                       │
│     现场比我想象中差一点。          │
│                                  │
└─────────────────────────────────┘

不再区分阶段图标(灯泡/沙漏/星星/对话气泡已删除),所有节点用同一种
中性圆点表示,只按时间顺序纵向排列,节点间保留"N天后"的间隔提示。
这个视图对"演唱会"这种一次性事件、和"学日语"这种长期主题,
渲染方式完全一样,不需要为不同类型的Thread做不同UI。
```

### UI 设计提示词
```
设计一个纵向时间线详情页,展示一条Thread下所有记录:
- 每个节点是统一样式的实心圆点(不区分类型/阶段),节点之间用竖线连接
- 节点之间的连接线中段插入一小段灰色文字,显示"N天后"的间隔
- 每个节点右侧是日期(小字)和记录原文(正文字号)
- 顶部标题是Thread的名字,副标题显示"共N条记录·跨度N天"
- 整体风格保持极简,不用卡片阴影,不用渐变,所有节点视觉权重相同
```

---

## 三点五、"关联到思维线"选择器(链接是如何建立的)

分两种触发路径,数据落地完全一致:

**路径一:输入框上方的"最近"快捷胶囊**——默认展示2-3条最近活跃的Thread,
点一下即可让即将发送(或已发送)的记录关联上去,这是最高频、成本最低的路径,
覆盖大多数场景(大部分关联都是关联到"最近还在想的那件事"上)。

**路径二:完整选择器**——点击⚭图标,或在快捷胶囊里点"更多",
弹出可搜索的完整Thread列表,用于关联到不常用的老Thread,或新建一条Thread。

```
┌─────────────────────────────┐
│  关联到思维线                  │
├─────────────────────────────┤
│  🔍 搜索已有思维线...          │
├─────────────────────────────┤
│  演唱会                 最近 │
│  学日语                 最近 │
│  要不要辞职                   │
│  副业计划                     │
├─────────────────────────────┤
│  + 新建思维线                  │
└─────────────────────────────┘
```

选中即完成关联,**不再有第二屏的阶段选择**——这是与上一版最大的区别。

### UI 设计提示词
```
设计一个"关联到思维线"的弹出选择器(底部弹出的Sheet样式):
- 顶部一个搜索框,可以搜索已有Thread
- 下方是Thread列表,最近使用的排在最前面并标注"最近"
- 列表底部一个"+ 新建思维线"入口,用虚线或不同底色区分于列表项,
  点击后是一个单行输入框,输入名字直接创建并立即完成关联
- 选中列表中任意一项后立即完成关联并关闭弹层,不设第二步选择
- 风格与其余页面一致:极简、无阴影渐变、中性色为主
```

---

## 四、桌面小组件(Home Screen Widget)

```
小尺寸(仅入口):          中/大尺寸(最新3条):
┌───────────┐            ┌─────────────────────┐
│           │            │ 最新几条              │
│    ✎+     │            │ 21:05 复盘下来还是... │
│  记一下    │            │ 15:30 尾盘又缩量了... │
│           │            │ 11:52 改主意了,想...  │
└───────────┘            │ ──────────  记一下 ✎+ │
                          └─────────────────────┘

限制:组件内不可输入文字(点击跳转App输入页,自动聚焦键盘);
组件内不可滚动(不做"查看全部",超出部分不显示,不提示还有几条)。
只展示时间倒序最新3条,不展示更早内容、不展示分支结构、不展示Thread关联。
```

### UI 设计提示词
```
设计两种尺寸的手机桌面小组件(参考系统时钟/备忘录风格小组件):
- 小尺寸:正方形,居中一个"添加"图标+"记一下"文字,整体可点击
- 中/大尺寸:圆角卡片,顶部小标题"最新几条",下面纵向列出3条记录
  (每条:灰色小号时间+单行文字,超长省略号截断),底部右对齐一个
  "记一下"文字+图标的次要按钮
- 风格与App内页面一致:扁平、无阴影渐变、中性色为主
```

---

## 五、API 设计(不含任何 NLP/AI 相关接口)

### 数据模型

```
Record 记录
- id: string
- text: string
- created_at: datetime
- parent_id: string | null        // 分支关系,指向父记录
- tag_id: string | null           // 事后手动打的标签,可选
- thread_id: string | null        // 手动关联的思维线,可选

Tag 标签
- id: string
- name: string
- color: string                   // hex颜色,用户自定义

Thread 思维线(跨时间的主题容器,取代原先的Event,不再区分阶段)
- id: string
- title: string                   // 例如"演唱会""学日语""要不要辞职"
- created_at: datetime
- last_used_at: datetime          // 用于"最近使用"排序,每次有新记录关联时更新
```

### 接口列表

```
# 记录 Record
POST   /records                   创建一条新记录
  body: { text, parent_id?, tag_id?, thread_id? }

GET    /records?date=YYYY-MM-DD   获取某天的所有记录(用于记录页/按日期回看)
  返回的每条记录,若 thread_id 不为空,附带 thread_title 字段,
  便于记录页直接渲染"🔗 思维线名"胶囊,无需额外请求

GET    /records/:id               获取单条记录详情
GET    /records/:id/children      获取某条记录的直接子分支(用于展开缩进)
PATCH  /records/:id               更新记录(补打标签、修改关联思维线等)
  body: { tag_id?, thread_id? }
DELETE /records/:id               删除一条记录

# 标签 Tag
POST   /tags                      创建自定义标签
  body: { name, color }
GET    /tags                      获取所有标签及各自的记录数
GET    /tags/:id/records          获取某标签下所有记录(按时间排序)
DELETE /tags/:id                  删除标签(不删除记录本身)

# 思维线 Thread
POST   /threads                   创建一条新Thread
  body: { title }
GET    /threads/recent?limit=3    获取最近活跃的N条Thread
  (按 last_used_at 倒序,用于记录页输入框上方的快捷胶囊)
GET    /threads?search=xxx        搜索/获取所有Thread(用于完整选择器)
GET    /threads/:id               获取Thread详情
GET    /threads/:id/records       获取该Thread下所有记录,按时间正序
  (用于渲染"思维线详情页",每条记录附带与上一条的时间间隔,
  前端据此计算"N天后"展示,不需要后端单独计算)

# 桌面组件 Widget
GET    /widget/latest?limit=3     获取最新N条记录(时间倒序,供桌面组件展示)
```

### 关键设计说明

- **三种关系各自独立**:`parent_id`(结构分支)、`thread_id`(跨时间主题)、
  `tag_id`(事后性质标记),三者互不依赖,一条记录可以同时具备三种关系,
  也可以一个都不设置。
- **Thread 关联的两条路径共用同一接口**:无论是创建记录时直接带上
  `thread_id`,还是事后用 `PATCH /records/:id` 补关联,数据结构和接口
  完全一致,前端只是调用时机不同。
- **不设 stage 字段**:`Thread` 下的记录只按时间正序排列,不做阶段分类,
  "思维线详情页"对一次性事件和长期主题渲染方式完全相同。
- **"最近使用"胶囊靠 `last_used_at` 排序**,每次有新记录关联到某个Thread时
  更新该字段,`GET /threads/recent` 直接返回排序结果,前端无需额外计算。
- **桌面组件**只依赖一个只读接口 `/widget/latest`,不需要访问分支树或
  Thread数据,保持组件端逻辑最简单。
